int main() {
  // process the command line args

  // atoi function to convert string to int

  // initialize the values based on the command line arguments

  // run n sequential processes

  // re-initialize the values based on th ecommand line arguments

  // run n concurrent processes
}
